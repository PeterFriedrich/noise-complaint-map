// BASEMAP_URL: fill in after Phase 0 (R2 setup) when adding MapLibre basemap
// const BASEMAP_URL = "pmtiles://https://<your-r2-bucket>.r2.dev/edmonton.pmtiles";

const INITIAL_VIEW = {
  longitude: -113.4938,
  latitude: 53.5461,
  zoom: 10.5,
  pitch: 45,
  bearing: -10,
};

const HEX_RADIUS = 300;   // metres per hexagon
const HEX_ELEVATION_SCALE = 30;
